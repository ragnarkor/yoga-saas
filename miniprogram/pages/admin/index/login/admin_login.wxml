<!-- [AI_START TIMESTAMP=2025-01-25 19:45:00] -->
<view class="main-admin bg-admin">

	<view class="login shadow">

		<view class="pic"><text class="icon-rankfill text-admin"></text></view>
		<view class="hint">后台管理系统</view>

		<!-- 选馆信息 -->
		<view class="tenant-info" bindtap="bindSwitchTenantTap">
			<text class="icon-home text-admin"></text>
			<text class="tenant-name">{{tenantName || '未选择场馆'}}</text>
			<text class="icon-angle-right text-grey switch-icon"></text>
		</view>

		<van-cell-group inset custom-class="login-form">
			<van-field
				value="{{ phone }}"
				label="手机号"
				type="number"
				placeholder="请输入管理员手机号"
				maxlength="11"
				data-field="phone"
				bind:change="bindFormChange"
			/>
			<van-field
				value="{{ pwd }}"
				label="密码"
				type="password"
				placeholder="请输入管理员密码"
				maxlength="30"
				data-field="pwd"
				bind:change="bindFormChange"
			/>
		</van-cell-group>

		<view class="form-submit-wrap">
			<van-button type="primary" block bind:click="bindLoginTap">点击登录</van-button>
		</view>

		<view bindtap="bindBackTap" class="return text-grey"><text class="icon-back"></text>返回</view>

	</view>

	<view class="foot-bottom">
		<cmpt-foot />
	</view>

</view>
<!-- [AI_END LINES=47 TIMESTAMP=2025-01-25 19:45:00] -->
