@import "../coach_common.wxss";

.marketing-hero {
  position: relative;
  height: 340rpx;
  overflow: hidden;
  background: #f0ebe3;
}

.marketing-hero-bg {
  width: 100%;
  height: 100%;
  display: block;
}

.marketing-hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to bottom,
    rgba(0, 0, 0, 0.04) 0%,
    rgba(0, 0, 0, 0.22) 100%
  );
  pointer-events: none;
}

.marketing-hero-text {
  position: absolute;
  left: 32rpx;
  right: 32rpx;
  bottom: 88rpx;
  z-index: 1;
}

.marketing-hero-title {
  display: block;
  font-size: 38rpx;
  font-weight: 700;
  color: #fff;
  line-height: 1.35;
  text-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.18);
}

.marketing-hero-sub {
  display: block;
  margin-top: 8rpx;
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.88);
}

.marketing-body {
  margin-top: -76rpx;
  padding: 0 24rpx;
  position: relative;
  z-index: 2;
}

.marketing-body .coach-revenue-card {
  margin: 0 0 20rpx;
}
