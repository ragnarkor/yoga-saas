/* [AI_START TIMESTAMP=2025-01-26 10:35:00] */
.coach-tab-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  height: calc(100rpx + env(safe-area-inset-bottom));
  padding-bottom: env(safe-area-inset-bottom);
  background: rgba(255, 255, 255, 0.95);
  display: flex;
  z-index: 999;
  box-shadow: 0 -4rpx 24rpx rgba(0, 0, 0, 0.06);
}

.coach-tab-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: var(--neutralTextTertiary, #8f959e);
  font-size: 22rpx;
  gap: 6rpx;
  position: relative;
  transition: color var(--transition-base, 0.25s ease);
}

.coach-tab-item:active {
  opacity: 0.7;
}

.coach-tab-item.active {
  color: var(--themeColor, #5b8a72);
}

.coach-tab-item.active::before {
  content: "";
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 48rpx;
  height: 6rpx;
  border-radius: 0 0 6rpx 6rpx;
  background: var(--themeColor, #5b8a72);
  animation: coachTabIndicator 0.3s ease-out;
}

@keyframes coachTabIndicator {
  from {
    width: 0;
    opacity: 0;
  }
  to {
    width: 48rpx;
    opacity: 1;
  }
}

.coach-tab-text {
  line-height: 1;
  transition: font-weight var(--transition-base, 0.25s ease);
}

.coach-tab-item.active .coach-tab-text {
  font-weight: 600;
}
/* [AI_END LINES=18 TIMESTAMP=2025-01-26 10:35:00] */
