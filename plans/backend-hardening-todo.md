# 后端加固 TODO

> 每项完成后必须：补测试、执行 `npm test`、记录涉及文件。

- [x] 请求级租户上下文，消除并发请求共享 `global.PID`。
- [x] 管理员 token 与当前租户匹配；超级管理员保留跨租户权限。
- [x] 排课教练时段冲突校验拆分为独立模块并测试。
- [x] 预约创建使用云数据库事务校验重复预约与时段容量。
- [x] 次数卡条件原子扣减、预约扣卡幂等、退款流水原子认领。
- [x] 扣卡失败时取消刚创建的预约，释放名额。
- [~] 将预约创建、扣卡、扣卡流水合并为单一云数据库事务（已接入代码，待云端联调验证）。
- [x] 将卡有效性与激活规则从 `user_card_service.js` 拆分为独立模块。
- [x] 将卡务扣减/退款流水从 `user_card_service.js` 拆分为独立模块。
- [x] 拆分 `admin_meet_service.js` 的课表复制与预约名单职责。
- [~] 增加满额并发、扣卡失败回滚、重复退款的云数据库集成测试（已增加本地并发/回滚模拟，仍待云 SDK 实例验证）。
- [ ] 在微信云开发测试环境用两个账号执行并发联调。
